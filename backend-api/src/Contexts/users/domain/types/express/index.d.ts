/* import * as express from "express";

declare global {
    namespace Express {
      interface Request {
          body : {
            id?: string,
            email: string,
            password: string,
            username?: string,
            lastname?: string,
            user_type?: number,
            genre?: string,
          }
        
      }
    }
  } */

  